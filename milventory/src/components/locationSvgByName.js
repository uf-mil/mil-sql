import { deliveryTruckSvg } from './SVGs/delivery_truck';

/** Map location `name` (DB / JSON title) to raw SVG markup for map rendering. */
export const LOCATION_SVG_MARKUP_BY_NAME = {
  'To Be Delivered': deliveryTruckSvg
};
